{% extends 'layout.html.twig' %}
{% block content %}

<div class="container-fluid col-12 ">
    <div class="row justify-content-center mx-0 flex-nowrap">
        {% if pseudo %}
            <div class="col-4">
                <h3>Bon voyage, {{ pseudo }} !</h3>
            </div>
        {% else %}
        <form method="POST" action="">
            <div class="col-11">
                <input type="text" class="form-control" name="pseudo" placeholder="Pseudo">
            </div>
            <div class="col-12">
                <button type="submit" class="btn btn-primary sitDown">Sit down on the couch</a>
            </div>
        </form>
        {% endif %}
        <div class="col">
            {% include "Home/_grid.html.twig" %}
        </div>
    </div>
</div>
{% endblock %}







